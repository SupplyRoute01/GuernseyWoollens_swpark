# 다정한 하루 디자인 방향

## North Star

반려동물과 함께 사는 사람이 포장지를 집어 들었을 때처럼, 따뜻하지만 성분과 과정은 분명한 브랜드 경험을 만든다. 한 화면의 목적은 방문자가 브랜드의 태도를 이해하고 대표 제품과 연락처를 편하게 찾는 것이다.

## Audience and tone

- 대상: 원재료와 소량 생산 여부를 꼼꼼히 보는 반려동물 보호자
- 어조: 다정하고 담백한 존댓말. 과장된 효능이나 의료 표현은 사용하지 않는다.
- 콘텐츠: 제품명, 가격, 사업자 정보, 이메일은 현재 임시 문구이며 공개 전에 실제 정보로 교체한다.

## Visual system

- `--pine #183e36`: 신뢰, 본문 강조, 주요 버튼
- `--sage #dce7d7`: 자연스러운 보조 면
- `--sage-light #f5f6ee`: 전체 배경
- `--peach #ffb79e`: 다정한 온도와 핵심 강조
- `--sun #f2cf72`: 재료와 수제 감각을 표현하는 제한적 포인트
- `--ink #20302c`: 기본 본문

표제는 한국어 명조 계열(`Noto Serif KR`, `Batang`)을 우선하고, 본문은 고딕 계열 시스템 글꼴을 사용한다. 웹폰트 다운로드에 의존하지 않아 정적 호스팅에서도 레이아웃이 안정적으로 유지된다.

## Layout and signature

넓은 화면에서는 설명과 제품 인장을 나란히 배치하고, 모바일에서는 한 열로 흐른다. 기억점은 실제 수제 간식 포장 라벨을 떠올리게 하는 복숭아색 타원형 제품 인장과 손으로 붙인 듯한 작은 정보 라벨이다. 다른 장식은 억제해 이 요소에 시선을 집중한다.

## Interaction and accessibility

- 네이티브 링크와 버튼을 사용하고 모든 상호작용에 키보드 포커스를 제공한다.
- 모바일 메뉴는 Escape로 닫히며 상태와 접근 가능한 이름이 함께 바뀐다.
- `prefers-reduced-motion`에서는 스크롤 모션을 제거한다.
- 최소 본문 크기는 16px을 기준으로 하고, 장식은 정보 전달에 의존하지 않는다.
- 스크롤바는 숨기지 않으며 고대비 모드에서 시스템 색을 따른다.

## Editing guide

브랜드명은 `다정한 하루`, 대표 이메일은 `hello@example.com`, 인스타그램은 `@yourbrand`로 검색해 교체한다. 제품과 이야기 카드는 `index.html`의 각 `<article>` 단위로 복제하거나 삭제할 수 있다.

