# 건지울른스 디자인 방향

## North Star

건지 섬의 항구에서 한 벌의 스웨터를 살펴보는 경험을 만든다. 단순히 오래된 느낌을 내는 빈티지가 아니라, 1976년부터 이어진 제작 태도와 나그랑 구조를 현대적인 편집 감각으로 보여준다. 방문자는 브랜드의 배경을 이해하고 코튼 나그랑 크루넥의 소재·구조·문의 경로를 빠르게 찾을 수 있어야 한다.

## Audience and tone

- 대상: 유행보다 옷의 배경, 소재, 봉제 구조와 오래 입는 가치를 보는 고객
- 어조: 차분하고 단정한 존댓말. 과장된 효능이나 확인되지 않은 제작 정보는 사용하지 않는다.
- 출처: 브랜드 연혁과 제품 이미지는 사용자가 제공한 서플라이루트 제품 페이지를 기준으로 한다.
- 임시 콘텐츠: 가격, 재고, 상세 사이즈, 사업자 정보는 공개 전에 실제 정보로 교체한다.

## Visual system

- `--navy #101b2b`: 바다, 신뢰, 주요 배경과 버튼
- `--navy-soft #1b2a40`: 네이비 보조 면
- `--mist #e7e9e4`: 항구의 흐린 빛, 제품 배경
- `--canvas #f2f0e9`: 오래된 작업 문서의 종이색을 절제해 해석한 기본 배경
- `--rope #c4ad82`: 로프와 자연 섬유를 연상하는 제품 정보 면
- `--signal #b64132`: 항구 신호기와 스카프에서 가져온 제한적 강조색

색상 토큰은 `styles.css`의 `:root`가 유일한 런타임 소유자다. 표제는 한국어 명조 계열을 사용하고, 좌표·연도·스펙은 좁은 산세리프 계열로 구분한다. 외부 웹폰트를 다운로드하지 않아 정적 호스팅에서도 레이아웃 이동을 막는다.

### Product detail system

`product-detail.html`은 포터리 제품 상세페이지의 이미지 우선 정보 구조를 참고하되, 포터리의 로고·카피·고유 표현은 가져오지 않는다. 큰 갤러리, 우측 고정 구매 정보, 긴 디테일 이미지와 스펙 원장이라는 구성 원리만 사용한다.

- 아이보리 `#f3efe4`: 상세페이지의 기본 면과 이미지 사이 여백
- 딥그린 `#17382f`: 본문, 선, 구매 버튼, 마지막 CTA
- 코퍼 브라운 `#a7653f`: 편직 라벨, 좌표, 제한적인 강조

상세페이지의 색상은 `styles.css`의 `.product-detail-page` 변수 재정의가 런타임 원본이다. `DESIGN.md`는 같은 값을 설명하는 문서이며 별도 토큰 생성기는 두지 않는다. 제품 컬러칩은 판매 옵션 정보이므로 세 가지 UI 색상 체계와 분리한다.

상세페이지 제목은 가늘고 큰 고딕으로 설정하고, 상품 코드·캡션만 좁은 산세리프를 쓴다. 카드 모서리와 장식적 그림자는 사용하지 않으며, 1px 선과 화면 높이 기준 여백으로 위계를 만든다.

## Layout and signature

첫 화면은 설명과 실제 항구 착용 사진을 비대칭으로 나누고, 경계에 나그랑 봉제선을 닮은 대각선을 둔다. 섹션 라벨, 좌표, 필드 노트는 항구 장비의 식별 표기를 참고한다. 가장 큰 시각적 기억점은 페이지를 가르는 대각선 구조이며, 앵커 아이콘과 레드는 이 구조를 보조하는 정도로만 사용한다.

제품은 여러 상품처럼 꾸미지 않고 한 벌을 세 가지 거리에서 보여준다. 정면은 실루엣, 넥라인은 나그랑 구조, 확대 이미지는 편직 밀도를 설명한다.

제품 상세페이지의 기억점은 왼쪽 화면 가장자리에 세로로 놓인 “편직 라벨”이다. 데스크톱에서만 보이며 구매 정보보다 강하게 보이지 않는다. 모바일에서는 첫 제품 사진 다음에 구매 정보가 바로 이어지고, 보조 갤러리는 아래 편집 섹션에서 다시 제공한다.

## Interaction and accessibility

- 모든 이동은 네이티브 링크와 버튼을 사용한다.
- 모바일 메뉴는 상태에 따라 접근 가능한 이름이 바뀌고 Escape로 닫힌다.
- 이미지에 고정 비율과 실제 너비·높이를 제공해 로딩 중 레이아웃 이동을 줄인다.
- `prefers-reduced-motion`에서는 스크롤과 등장 모션을 제거한다.
- 포커스 표시, 고대비 모드, 전역 스크롤바 스타일을 제공한다.
- 최소 본문 크기는 16px을 기준으로 하고 정보는 색상이나 장식만으로 전달하지 않는다.

## Editing guide

- 브랜드명: `GUERNSEY`, `건지울른스`
- 대표 제품명: `코튼 나그랑 크루넥 스웨터`
- 이메일: `support@supplyroute.co.kr`
- 제품 정보: `index.html`의 `product-spec-table`
- 블로그 임시 글: `journal-list` 안의 각 `<article>`
- 이미지 교체: `assets/guernsey-*.jpg` 파일명을 유지하면 마크업 수정 없이 교체할 수 있다.
- 공통 로고: `assets/guernsey-logo.png`를 공개 페이지 헤더와 푸터에서 사용하며, 560×300 가로형 원본 비율을 유지해 축소한다.
- 히어로 영상: 홈 첫 화면은 무음 자동재생 영상을 사용한다. `assets/hero-scrub.mp4`, `assets/hero-scrub-m.mp4`와 각 포스터 이미지는 Git에 포함되어 정적 호스팅에서 직접 접근 가능해야 한다.
- 제품 데이터: `products.json`에 `name`, `price`, `image`, `url`을 추가하면 제품 페이지와 홈 대표 제품 목록이 자동 갱신된다.
- 공개 제품 목록은 제품명에 `건지울른스`가 포함된 항목만 사용하며, 홈은 그중 앞의 3개를 자동 선택한다.
- 장바구니: `assets/cart.js`와 `assets/cart.css`를 모든 공개 HTML에서 로드한다. 제품 카드 버튼은 `products.json`의 `name`, `price`, `url`을 `data-cart-add` 속성으로 자동 연결한다.
- 대표 제품 상세: `product-detail.html`의 구매 링크와 제품 정보, `styles.css`의 `.product-detail-*` 규칙
- 상세페이지 관련 제품: `products.json`에서 “건지울른스 코튼 나그랑 크루넥 스웨터 -”가 포함된 컬러 제품 중 3개를 자동 표시한다.

## About page narrative

- `about.html`은 창업자 인터뷰를 바탕으로 한 1인칭 브랜드 스토리다.
- 서사 순서: 2019년 도쿄에서의 발견 → 첫 시즌의 위기 → 원형을 지키는 원칙 → 고객의 니트 복원 → 오래 남기겠다는 약속.
- 핵심 헤드라인은 “여기서는 살 수 없어서, 길을 만들기 시작했습니다.”이며 `Supply Route`라는 이름의 의미와 직접 연결한다.
- 대표 사진은 현재 `assets/guernsey-harbour.jpg`를 임시 사용한다. 창업자 또는 팀 사진이 준비되면 같은 세로 프레임으로 교체한다.
- 인터뷰의 구체적인 장면과 말투를 유지하며, 기업 보도자료처럼 3인칭으로 바꾸지 않는다.

## Story integration

- 블로그는 `story/` 아래에 두고 공통 `../styles.css`, `../script.js`를 사용해 본 사이트와 헤더·네비게이션·푸터·모바일 메뉴를 공유한다.
- 글 본문은 가독성을 위해 최대 `680px`로 제한한다.
- `story/posts.json`의 샘플 글 2개는 유지하고, 홈은 날짜 내림차순 최신 3개를 자동 표시한다.
- `story/admin.html`은 블로그 키트 원본을 그대로 보존하며 디자인 통합 대상에서 제외한다.

## Contact integration

- `contact.html`은 이메일, SNS, 스토어의 세 경로를 아이콘 없는 대형 텍스트 버튼으로 보여준다.
- 모든 외부 채널은 새 탭으로 열고 `noopener noreferrer`를 함께 사용한다.
- 공개 페이지의 공통 푸터에는 이메일과 `@guernseywoollens_kr` 링크를 항상 노출한다.
